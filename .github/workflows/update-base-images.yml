# This Workflow performs a full release of che dashboard
name: Update base images
on:
  workflow_dispatch

jobs:

  build:
    runs-on: ubuntu-20.04
    steps:
      - 
        name: print output 
        run: |
          echo "testing job run"
          exit 1
  notify:
    if: always() && (needs.test.build.job_status=='failure' || needs.build.outputs.job_status=='success')
    runs-on: ubuntu-20.04
    needs: build
    steps:
      -
        name: "do if failure"
        if: needs.build.outputs.job_status=='failure'
        run: |
          echo "prepare failure notification" > notification
      -
        name: "do if success"
        if: needs.build.outputs.job_status=='success'
        run: |
          echo "prepare success notification" > notification
      -
        name: "display notification"
        run: |
          cat notificaion 
